# Windows 7 SP1 x64 build. Use the official CPython 3.8.10 x64 installer.
# Keep this separate from requirements.txt (the current-Windows build).
dearpygui==2.3.1
tomli==2.0.1

# Build tools, including transitive dependencies, frozen for reproducibility.
pyinstaller==6.16.0
pyinstaller-hooks-contrib==2026.7
altgraph==0.17.5
importlib-metadata==8.5.0
packaging==26.2
pefile==2023.2.7
pywin32-ctypes==0.2.3
setuptools==56.0.0
zipp==3.20.2
