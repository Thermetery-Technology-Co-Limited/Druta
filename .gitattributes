# Runtime provenance hashes must survive Windows checkouts unchanged.
# Diff context intentionally retains upstream whitespace and its leading space.
src/druta/tools/vista/*.patch text eol=lf -whitespace
